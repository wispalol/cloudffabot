/**
 * Database schema — reserved for future migrations.
 * All tables are currently created in database.js via CREATE TABLE IF NOT EXISTS.
 * This file can be used with a migration library (e.g. db-migrate) in the future.
 */
module.exports = {};
